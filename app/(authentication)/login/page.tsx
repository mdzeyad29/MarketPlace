import LoginPage from "@/components/authentication/loginPage";

const Login= () => {
    return (
        <div>
            <LoginPage />
        </div>
    );
};

export default Login;               